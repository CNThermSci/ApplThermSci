#!/bin/bash

for F in \
        .gitignore \
        Makefile \
        bib \
        bibfile.bib \
        A0702-pt.tex \
        A0702-pt-PRES.tex \
        A0702-pt-HOUT.tex \
        gitter; do
    git add $F && git commit -m "ADD the $F"
done

git push

