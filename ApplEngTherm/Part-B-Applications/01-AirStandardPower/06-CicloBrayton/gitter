#!/bin/bash

for F in \
        .gitignore \
        Makefile \
        bib \
        bibfile.bib \
        B0106-pt.tex \
        B0106-pt-PRES.tex \
        B0106-pt-HOUT.tex \
        gitter; do
    git add $F && git commit -m "ADD the $F"
done

git push

