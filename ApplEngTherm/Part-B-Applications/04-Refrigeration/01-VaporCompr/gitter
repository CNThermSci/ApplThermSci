#!/bin/bash

for F in \
        .gitignore \
        Makefile \
        bib \
        bibfile.bib \
        B0401-pt.tex \
        B0401-pt-PRES.tex \
        B0401-pt-HOUT.tex \
        gitter; do
    git add $F && git commit -m "ADD the $F"
done

git push

